import { SxProps } from "@mui/material";

export const createCommunityBoxSxProps: SxProps = {
    width: "70%",
    margin: "auto",
    textAlign: "center",
    display: "flex",
    flexDirection: "column",
    justifyContent: "center",
    gap: "1rem",
}